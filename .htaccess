Options -Indexes
RewriteEngine On

# Encaminha requisicoes para a pasta public/, independente do nome da subpasta.
# Ex.: /adsolutions -> /adsolutions/public
RewriteCond %{REQUEST_URI} !/public/
RewriteRule ^(.*)$ public/$1 [L]
